import {HttpClientInterface} from "../adapters/HttpClientInterface";
import {TYPES} from "../../infra/types/Types";
import {inject, injectable} from "inversify";

@injectable()
export class EmailInboundSyncDatabase {
    
}
